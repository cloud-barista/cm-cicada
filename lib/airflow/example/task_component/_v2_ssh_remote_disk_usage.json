{
  "name": "_v2_ssh_remote_disk_usage",
  "description": "[V2 Sample] 원격 호스트의 디스크 사용량 확인 (SSHOperator). ssh_conn_id에 해당하는 SSH connection을 Airflow에 미리 등록해야 함.",
  "type": "ssh",
  "spec": {
    "ssh_conn_id": "example_remote_host",
    "command": "df -h --output=source,size,used,avail,pcent,target | sort -k5 -hr"
  }
}
